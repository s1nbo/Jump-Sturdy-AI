#include "ai.hpp"

/*
class ai {
public:
    ai() {}

    int choose_move(std::vector<int> board) {
        return 0;
    }

    std::unordered_map<int, int, int, int> move_rating(std::vector<int> board, std::vector<int> moves) {
        std::unordered_map<int, int, int, int> ans;
        for (int move : moves) {
            ans[move] = 0;
        }
        return ans;
    }
};
*/